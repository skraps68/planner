apiVersion: v1
kind: Service
metadata:
  name: planner-app-service
  namespace: planner-app
  labels:
    app: planner
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: planner
